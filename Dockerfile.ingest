FROM python:3.11-slim
WORKDIR /app

# Install dependencies
COPY requirements.txt ./
RUN pip install --no-cache-dir -r requirements.txt

# Copy service files
COPY ingest.py ./
COPY generate_pdf.py ./

ENV PORT=8080
EXPOSE 8080

CMD ["uvicorn", "ingest:app", "--host", "0.0.0.0", "--port", "8080"]
